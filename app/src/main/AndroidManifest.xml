<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpertMaintenance"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Google Maps API Key - Supprimé car non nécessaire pour le moment -->
        <!-- À ajouter quand vous aurez une API Key Google Maps -->


        <!-- Login Activity -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ExpertMaintenance.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.ExpertMaintenance.NoActionBar" />

        <!-- Intervention Details Activity -->
        <activity
            android:name=".ui.details.InterventionDetailsActivity"
            android:exported="false"
            android:label="@string/title_activity_intervention_details"
            android:parentActivityName=".ui.MainActivity"
            android:theme="@style/Theme.ExpertMaintenance.NoActionBar" />

        <!-- Intervention Edit Activity -->
        <activity
            android:name=".ui.edit.EditInterventionActivity"
            android:exported="false"
            android:label="Modifier l'intervention"
            android:parentActivityName=".ui.details.InterventionDetailsActivity"
            android:theme="@style/Theme.ExpertMaintenance.NoActionBar" />

        <!-- Image Capture Activity -->
        <activity
            android:name=".ui.ImageCaptureActivity"
            android:exported="false"
            android:label="@string/title_activity_image_capture"
            android:parentActivityName=".ui.InterventionDetailsActivity"
            android:theme="@style/Theme.ExpertMaintenance.NoActionBar" />

        <!-- Map Activity - Commenté car n'existe pas encore -->
        <!--
        <activity
            android:name=".ui.MapActivity"
            android:exported="false"
            android:label="@string/title_activity_map"
            android:parentActivityName=".ui.InterventionDetailsActivity"
            android:theme="@style/Theme.ExpertMaintenance" />
        -->

        <!-- History Activity -->
        <activity
            android:name=".ui.history.HistoryActivity"
            android:exported="false"
            android:label="@string/title_activity_history"
            android:parentActivityName=".ui.InterventionDetailsActivity"
            android:theme="@style/Theme.ExpertMaintenance.NoActionBar" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:label="Paramètres"
            android:parentActivityName=".ui.MainActivity"
            android:theme="@style/Theme.ExpertMaintenance.NoActionBar" />

        <!-- About Activity -->
        <activity
            android:name=".ui.about.AboutActivity"
            android:exported="false"
            android:label="À propos"
            android:parentActivityName=".ui.settings.SettingsActivity"
            android:theme="@style/Theme.ExpertMaintenance.NoActionBar" />

        <!-- Clients Activity -->
        <activity
            android:name=".ui.clients.ClientsActivity"
            android:exported="false"
            android:label="Clients"
            android:parentActivityName=".ui.MainActivity"
            android:theme="@style/Theme.ExpertMaintenance.NoActionBar" />



        <!-- File Provider for camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
